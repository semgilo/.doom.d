# key: behaviour
# name: behaviour
# --
-- `(file-name-nondirectory buffer-file-name)`
-- Created by `user-simple-name`, `(format-time-string "%Y-%m-%d")`
-- ${1:describle}

local PURE_FILE = `(file-name-base buffer-file-name)`;

`(file-name-base buffer-file-name)` = class("`(file-name-base buffer-file-name)`", BaseForm)

`(file-name-base buffer-file-name)`.__attribs = {
    -- gameObject = "GameObject"
}

function `(file-name-base buffer-file-name)`:onStart()
    `(file-name-base buffer-file-name)`.super.onStart(self);
end

function `(file-name-base buffer-file-name)`:onUpdate(dt)
     `(file-name-base buffer-file-name)`.super.onUpdate(self, dt);
end